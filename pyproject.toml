[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

# ===== METADATA ===== #

[project]
name = "norfair_rs"
dynamic = ["version"]
description = "Fast object tracking library - Rust implementation of norfair"
readme = "README.md"
license = { text = "BSD-3-Clause" }
keywords = ["tracking", "object-tracking", "kalman-filter", "computer-vision", "norfair"]
classifiers = [
    "License :: OSI Approved :: BSD License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Rust",
]
urls = { Homepage = "https://github.com/nmichlo/norfair-rs", Repository = "https://github.com/nmichlo/norfair-rs" }

# ===== DEPENDENCIES ===== #

requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.0",
    "maturin>=1.4",
]
test-compat = [
    "norfair==2.3.0",
    "opencv-python>=4.0",
]

[dependency-groups]
dev = [
    "maturin>=1.10.2",
    "norfair>=2.3.0",
    "pytest>=9.0.1",
    "pre-commit>=4.0.0",
    "ruff>=0.8.0",
]

# ===== MATURIN ===== #

[tool.maturin]
features = ["python"]
python-source = "python"
module-name = "norfair_rs._norfair_rs"

# ===== PYTEST ===== #

[tool.pytest.ini_options]
testpaths = ["python/tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

# ===== RUFF ===== #

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
# `E4/E7/E9 + F` is the ruff default, `I` adds import sorting (previously isort).
# `UP` modernises syntax for the python floor, including PEP 695 generics.
# `TID251` enforces the banned-api list below.
# `B009`/`B010` ban `getattr(x, "const")` / `setattr(x, "const", v)` -- write `x.const`.
select = ["E4", "E7", "E9", "F", "I", "UP", "TID251", "B009", "B010"]

[tool.ruff.lint.flake8-tidy-imports.banned-api]
"typing.Any".msg = "Any disables type checking wherever it appears. Use a precise type, a TypeVar, a Protocol, or a dedicated sentinel class."
"typing.cast".msg = "cast asserts a type the checker cannot verify. Narrow with isinstance/assert, or fix the annotation."

[tool.ruff.lint.isort]
# previously `isort --profile=black --force-single-line-imports`
force-single-line = true

[tool.ruff.lint.per-file-ignores]
# Test files can have unused imports (for skipped tests) and style issues
"python/tests/*.py" = ["F821", "E721", "B007", "E711", "B905", "E501", "UP038"]
# Type stubs have long signatures
"*.pyi" = ["E501"]
# Example files - more lenient
"examples/**/*.py" = ["E501"]

[tool.ruff.format]
quote-style = "double"
